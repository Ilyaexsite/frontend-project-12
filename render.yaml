services:
  - type: web
    name: hexlet chat
    env: node
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    healthCheckPath: /api/v1/channels
    autoDeploy: true